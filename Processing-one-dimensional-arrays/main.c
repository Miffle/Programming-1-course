#include "posled.h"
#include "func1.h"
#include <stdio.h>


int main(){
    int n;
    printf("n= ");
    scanf("%d", &n);
    rando();
    funcQWE(n, A, Q);
    funcQWE(n, B, W);
    funcQWE(n, C, E);
    func2(n);
    viv(n);
    return 0;
}